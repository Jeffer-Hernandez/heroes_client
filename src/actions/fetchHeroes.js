export function fetchHeroes() {
    // fetch('http://localhost:3000/api/v1/heroes')
    // .then(resp => resp.json())
    // .then(data => console.log(data))

    // dispatchEvent() // will dispatch to the reducer, which will update the store
                    // can be dispatched within this action because of thunk

}